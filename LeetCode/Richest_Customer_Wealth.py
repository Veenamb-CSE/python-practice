class Solution:
    def maximumWealth(self, accounts: List[List[int]]) -> int:
        sumation=[]
        for i in range(0,len(accounts)):
            sum(accounts[i])
            sumation.append(sum(accounts[i]))
        return max(sumation)
        
